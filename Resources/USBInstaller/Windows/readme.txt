Download WIX from : 

wixtoolset.org

Do mvn clean install for the whole project.

Run build.bat with the desired version number, e.g. <build.bat 1.0.0>. It will fail, if there is no .exe available. 
